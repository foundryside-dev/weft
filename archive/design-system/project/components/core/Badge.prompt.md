**Badge** — a prominent status pill (the board's Open / Active / Done toggle, a primary state marker). Filled amber by default; `outline` for the inactive/neutral state. For small inline metadata (issue type, status word, severity) reach for `Tag` instead — Badge is the louder, larger sibling.

```jsx
<Badge>Open</Badge>
<Badge variant="outline">Done</Badge>
```

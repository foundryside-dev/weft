# Metrics — Weft Federation             Last read: 2026-07-01 — seam stewardship reclaimed (PDR-0045): hub owns the seam-index (contracts/seam-index.json), wardline conformance gate re-confirmed CI-only → enrich-only still 0; prior 06-29: both seam REDs landed (axis-2 confident-wrong seams → 0)

> Targets below are falsifiable-by-shape PLACEHOLDERS (number + date) for the owner
> to set real values against — most baselines are not yet instrumented. A directional
> word is not a metric; reject it. Kill/keep logic: product-metrics-and-experimentation.md.

## North-star
| Metric | Target (falsifiable) | Current | Read on | Trend |
|--------|----------------------|---------|---------|-------|
| **Dogfood pass rate** — % of planted lacuna defects an agent surfaces *and* tracks **using the suite, not grep** ("if the agent prefers grep, the tool failed") | ≥ 90% by 2026-07-31 *(placeholder — owner to set)* | **post-fix validation (2026-06-15): 16/17 MCP-surface green · 4/4 federation joins green · 8/8 structural lacunae reachable** (`~/lacuna/docs/dogfood/2026-06-15-postfix-validation-report.md`). Only residual: narrow Fix-B package-name-recursion bug `lacuna-522ab56124` (blocks no join). Correctness north-star is **essentially maxed.** **06-29 — UNCONFOUNDED wild datapoint:** a Codex agent in `~/elspeth` (a NON-member project — no federation-membership incentive, only the ambient MCP/CLI skills) reached for **four** weft tools unprompted: `filigree.work_start`/`filigree ready`, `loomweave.entity_find` (over grep, to locate `FailureInfo` + its tests by symbol), and also wardline + warpline. Read them differently: **filigree + loomweave = clean unconfounded prefer-suite-over-grep** (elspeth consumes them as a true outsider — member dogfood is biased by membership, so this is the cleaner test). **wardline + warpline = organic reach but confounded + thin** — elspeth is their *birthplace* (`~/elspeth/elspeth-lints/` → wardline), so not outside-adoption evidence. See [[weft-unconfounded-dogfood-from-nonmember]]. **06-29 — STRONGEST datapoint (`~/fable`, fully clean-room):** a random project idea handed to an agent with authority to "use any tools incl. the weft federation" — **zero genealogy to any member** → fully unconfounded. The agent organically chose **plainweave** to register the project's requirements (`req add`/`criterion add`/`approve`), *learning the contract by probing* (approve `--expected-version` 1→CONFLICT, tested 0→approved) then scripting the rest. The honest/discriminated error envelope is what made it learnable-by-probing → honesty is a north-star ENABLER, not just integrity ([[weft-honest-errors-make-tools-learnable]]). Polish surfaced (non-blocking, agent self-recovered): the CONFLICT error names no current version (say-what-you-know would cut a probe cycle); the `actor register` missing-`--actor` hint ("refresh state") misdirects. **The agent also SEQUENCED suite adoption by lifecycle fit** ("start with filigree and loomweave, but wardline/warpline wait until I've got more code") → the members have legible per-stage jobs (positioning is agent-legible); a half-seam with no consumer may be "not-yet" not "never." | 2026-06-29 | 1/4 → **4/4 joins** (pre-fix → post-fix); + clean-room plainweave adoption (fable) & outside-project prefer-grep-beaten (elspeth) |

## Input metrics (the levers that move the north-star)
| Metric | Target | Current | Read on |
|--------|--------|---------|---------|
| Time-to-orient for a resuming agent (boot → actionable "what's next") | ≤ TARGET *(set after baselining)* | not instrumented (Next-bet mechanism A builds this) | 2026-06-09 |
| **Supervision load** — how much the operator must *watch* per dispatched ticket (the real outcome the Next bet buys: fire-and-forget delegation; PDR-0004) | can dispatch in parallel **without babysitting** *(set a concrete proxy after baselining)* | BASELINE — operator is today the live deconfliction backstop; cannot fire-and-forget. **06-26:** the Tenter operating-picture spike (PDR-0034) demonstrated a concrete lever — it surfaces STALE-DONE / orphan / untracked gaps an operator reconstructs by hand today — but the metric itself is still un-instrumented; the full-Tenter bet that would move it is DEFERRED on fabric stability. | 2026-06-26 |
| **Stomp interventions** (proxy/input for supervision load) — operator catching an agent stomping another's in-progress work (the "oh-shit-ESC" event) | **→ 0** (after Next-bet mechanism B) | **BASELINE ~1/wk diligent · ~every 2–3 days impaired · never zero** (operator-reported; 3 observed cases incl. one live mid-discovery 2026-06-09; recovery now near-trivial — value is supervision-load, not catastrophe, per PDR-0004) | 2026-06-09 |
| Finding round-trip fidelity (wardline emit → filigree, suppression/baseline state preserved) | 100% of baselined findings carry provenance + are filterable | partial — N2 residual: 33 baselined findings still land `status:open` (`weft-171fc22a50`) | 2026-06-09 |

## Guardrails (must NOT degrade)
| Metric | Floor / ceiling | Current | Read on |
|--------|-----------------|---------|---------|
| **Enrich-only invariant** — cross-member hard-blocks (a sibling's absence breaking another's core flow) | ceiling = 0 | 0 known load-bearing blocks (F4 closure-gate watched). **06-24:** post-launch ships (loomweave 1.3.1, wardline 1.0.6, legis 1.1.1, warpline 1.2.0) all **additive/patch** — no breaking cross-member contract; wardline now **fail-soft on Loomweave emit errors** (strengthens enrich-only). No reversal trigger fired. **06-26:** members continue shipping additive/patch (filigree 3.1.0 — warpline reverse-lookup + per-issue commit-anchor, schema v29; legis 1.2.0-prep — per-SEI attestation classifier; plainweave 1.0.0; loomweave Rust-plugin fixes; wardline 1.0.7). No breaking cross-member contract; guardrail still 0 (filigree v29 + commit-anchor flagged for a blast-radius confirmation pass). **06-28:** two new enrich-only consumer fills landed on branches — Loomweave churn consumer (reads warpline's frozen churn; warpline-absent → honest-empty + reason, never empty-as-clean) and Legis←Plainweave advisory preflight (legis governance verdicts proven **byte-identical** with plainweave None vs a *hostile* client — the advisory boundary holds). Both consume a frozen contract, create no sibling obligation. Guardrail still **0**. **06-29:** the 7-member federation interface+gap audit (`pm/2026-06-29-federation-interface-gap-map.md`) checked enrich-only explicitly per member and confirmed **0 load-bearing cross-member deps** — every consumed seam has a cited honest-degrade path. One fail-closed/load-bearing-shaped seam (filigree→loomweave registry for *scan-ingest*) is scoped to the federation feature, not core issue-tracking (flagged AMBER-8 to confirm). No reversal trigger fired; guardrail still **0**. Separately, the "every seam lost the ability to say I don't know" thesis (06-15) is now **cured** (failed:[] fixed, scheme-drift nameable on the filigree wire, discriminated reasons) — the two remaining confident-wrong seams were FIXED+LANDED 2026-06-29 (PDR-0041): RED-1 `weft-0678843f13` closure-gate drift → filigree main `9b50143` (discriminated-UNKNOWN degrade verified in-code); RED-2 `weft-d5091cba12` plainweave boundary-lie → plainweave main `9b5fc88`. **Axis-2 confident-wrong seams → 0.** **07-01:** the seam-conformance program is RECONCILED (PDR-0045) — the hub reclaimed the seam-index canon (`contracts/seam-index.json`) + dissolved wardline's self-appointed steward role (PR #86 merged; `git grep -i steward`==0 on wardline main). The wardline conformance gate was re-confirmed **CI-only** (0 `src/` refs; asserts structure only), so no runtime cross-member dep entered and PDR-0038's reversal trigger did not fire — guardrail still **0**. | 2026-07-01 |
| **Tree cleanliness** — members dirtying the working tree at rest (blocks legis signing) | ceiling = 0 | 1 open: loomweave runtime DB under `.weft/` (`weft-d822a7de2d`) | 2026-06-09 |
| **Finding duplication** — duplicate finding rows from fingerprint drift | ceiling = 0 | wardline WL-1 breach (`weft-4a9d0f863c`) still open; **loomweave's instance FIXED 2026-06-13** (B10 `weft-7256739b31`: site-keyed secret-finding identity + incremental sweep, self-heals existing dupes on next analyze) | 2026-06-13 |
